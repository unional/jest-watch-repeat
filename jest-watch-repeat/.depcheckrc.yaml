# One package per line. A wildcard here hides dead dependencies.
ignores:
  - '@types/node'
  # Supplies the coverage provider loaded by vitest.config.ts.
  - '@vitest/coverage-v8'
  # Runs jest.smoke.config.mjs, the in-situ check that a real jest can still
  # load the built plugin. Also the peer this package is a plugin for.
  - jest
  # bin-only, referenced from scripts
  - npm-run-all2
  - rimraf
  - tsdown
